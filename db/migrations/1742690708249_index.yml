database:
  engine: postgres
schema:
  version: 1742690708249
  name: index
  tables:
    users:
      columns:
        id:
          type: SERIAL
          constraints:
            - PRIMARY KEY
        first_name:
          type: VARCHAR(100)
          constraints:
            - NOT NULL
        last_name:
          type: VARCHAR(100)
          constraints:
            - NOT NULL
        email:
          type: VARCHAR(255)
          constraints:
            - NOT NULL
            - UNIQUE
        password_hash:
          type: VARCHAR(255)
          constraints:
            - NOT NULL
        created_at:
          type: TIMESTAMP WITH TIME ZONE
          default: CURRENT_TIMESTAMP
        updated_at:
          type: TIMESTAMP WITH TIME ZONE
          default: CURRENT_TIMESTAMP
      indexes: []
      foreign_keys: []
